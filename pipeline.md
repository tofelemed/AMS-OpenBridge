# Pipeline Validation — Running Bug Log

**Run started:** 2026-08-13
**Scope:** End-to-end pipeline validation per `docs/migration/11-pipeline-validation.md` (execution prompt).
**Stack:** local Docker Compose lab (`infra/docker/docker-compose.yml`), 34 containers up.
**Synthetic load:** `ams-sims/` (new suite created in this run).

Severity: Critical (pipeline broken end-to-end) / High (one stage broken, workaround exists) /
Medium (data quality / edge case) / Low (cosmetic / log noise).
Category: `auto-fix` (applied + re-verified in this run) / `flag` (proposed fix only — needs approval).

## Bug log

| ID | Pipeline/Stage | Symptom | Evidence (file:line / log excerpt / topic+offset) | Severity | Category (auto-fix / flag) | Fix Applied | Fix Description | Re-verified | Approval Needed |
|----|----------------|---------|----------------------------------------------------|----------|------------------------------|--------------|------------------|-------------|------------------|
| PIPE-001 | Pre-flight / Kafka host access | Published Kafka host port 9093 is unusable by host clients: bootstrap connects but metadata returns an unresolvable broker address, so every host producer/consumer fails with `Local: Broker transport failure` | `infra/docker/docker-compose.yml` kafka env: `KAFKA_ADVERTISED_LISTENERS=INTERNAL://kafka:9092,EXTERNAL://:9093` (EXTERNAL advertises an **empty host** → client resolves the container's canonical hostname, unreachable from Windows host). Repro: `python -c "AdminClient({'bootstrap.servers':'localhost:9093'}).list_topics(timeout=5)"` → `KafkaError{code=_TRANSPORT,...}` | Medium | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | Compose now advertises `EXTERNAL://${KAFKA_EXTERNAL_HOST:-127.0.0.1}:9093` (127.0.0.1, not `localhost` — Windows resolves `localhost` to ::1 first and Docker's published port black-holes IPv6, which re-broke delivery on the first attempt). Broker recreated with the new env | **Yes** — host `confluent_kafka` lists 35 topics AND delivers a produce (`raw-alarms-dlq` p1 offset 59) via `127.0.0.1:9093` (`ams-sims/_host_kafka_check.py`) | No |
| PIPE-002 | Pre-flight / ACK loop | External DCS ACK endpoint unreachable from this lab → `ACK_CONFIRMED` can never be produced by the real HTTP writeback; every real ACK round-trip will end `ACK_FAILED` | `Test-NetConnection 192.168.1.51 -Port 8010` → `TcpTestSucceeded: False`; failure branch `HttpAckWritebackService.cs:143-148` | Medium | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | New compose service **`mock-dcs`** (python:3.11-alpine stub, answers 200 + logs every ACK body); `AlarmIngestion__AckWritebackUrl` → `${ACK_WRITEBACK_URL:-http://mock-dcs:8010/api/alarms/acknowledge}` (env-overridable back to a real DCS). Bonus: the at-least-once writeback consumer immediately **redelivered the morning's 3 uncommitted vack5 ACKs** to the stub on startup — STR-10 semantics observed working | **Yes** — `sim_ack_lifecycle` run-tag `rack2` 10/10: real HTTP round trip now ends `ACK_CONFIRMED` (mock-dcs log shows the ACK bodies; `ack-results` carries `ACK_CONFIRMED`; Postgres `ack_status=t` via the Kafka round-trip in <2 s) | No |
| PIPE-003 | Pre-flight / CPLM consumers | **RESOLVED — documentation shorthand, not a system defect.** The event-frames group is named `ams-api-cplm-results-frames` (code builds it as `ConsumerGroupId + "-frames"`, `CplmEventFrameService.cs:66`), so the docs' "`ams-api-cplm-results`, `-frames`" shorthand misleads readers into probing `ams-api-cplm-frames`, which never exists. Both real groups verified **singleton**: `ams-api-cplm-results` (1 member, `-sole-` static instance) and `ams-api-cplm-results-frames` (1 member, `-frames-sole-` static instance, lag 0 after consuming the run's 12 gate results) | `CplmResultConsumerService.cs:531` (`ams-api-cplm-results` default) + `CplmEventFrameService.cs:66-68`; `kafka-consumer-groups --list/--describe` output 2026-08-13 | Low (doc) | flag → **fixed 2026-08-13 (fix round)** | **Yes** | CLAUDE.md now spells out both full group names (+ the `ConsumerGroupId + "-frames"` derivation); the runbook already used full names. Stale-doc note on `lifecycle-alerts` (no longer an orphan — notification-service consumes it, lag 0) stands for the arch-review docs | **Yes** — doc text verified | No |
| PIPE-004 | Stage 1 / Kafka topic audit | 3 catalog topics running on broker defaults instead of their declared configs: `flink.state.alarm.replay`, `analysis.executions`, `analysis.results` had **no** dynamic config → broker default retention **24 h** (`KAFKA_LOG_RETENTION_HOURS=24`) instead of the declared 7 d, and no explicit `cleanup.policy`. Replay/analysis data silently expiring 6 days early | `scripts/kafka-reset-lab-topics.ps1:33-37` declares `retention.ms=604800000,cleanup.policy=delete` for all three; live `kafka-configs --describe` showed no overrides (`cleanup.policy=BROKER_DEFAULT` in audit sweep). These topics were added to the reset script 2026-08-12 (in-file comment) — live topics predate that and were never re-aligned | Medium | auto-fix | **Yes** | `kafka-configs --alter --add-config retention.ms=604800000,segment.ms=3600000,cleanup.policy=delete,min.insync.replicas=1` applied to each (non-destructive, same operation the reset script's ensure-tier performs; no broker restart, no data loss) | **Yes** — `--describe` now shows `retention.ms=604800000`, `cleanup.policy=delete`, `segment.ms=3600000`, `min.insync.replicas=1` as DYNAMIC_TOPIC_CONFIG on all three | No |
| PIPE-005 | Stage 1 / Kafka broker config | Broker runs with `KAFKA_AUTO_CREATE_TOPICS_ENABLE=true`, contradicting the recorded Plan 09 decision ("Plan 09 turns auto-create off" — comment in `scripts/kafka-reset-lab-topics.ps1:32`, also relied on in `docs/alarm-history-flink-sink-stuck.md`). With auto-create on, a typo'd topic name silently materializes a 4-partition/24 h-retention topic instead of failing loudly — the exact failure class Plan 09 aimed to remove | `infra/docker/docker-compose.yml:371` `KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"`; live broker env confirms. `audit-events` + `lifecycle-alerts` existing with pure broker-default configs (no dynamic config at all) is consistent with them having been auto-created | Medium | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | `KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"` in compose; broker recreated. Immediately earned its keep: it surfaced `live.alarm.metrics` as an uncataloged topic that only ever existed by auto-creation (→ PIPE-013) | **Yes** — broker env confirms `false`; produce to a nonexistent topic stays undelivered and creates nothing (`kafka-topics --list` grep = 0) | No |
| PIPE-006 | Stage 1 / Kafka topic audit | `audit-events` (in the 04-data-flows catalog, produced by display-service/cplm-api, consumed by audit-service) is missing from `scripts/kafka-reset-lab-topics.ps1` entirely and lives on broker defaults (24 h retention). A stack reset would drop/never recreate it correctly; audit transport window is 24 h. `lifecycle-alerts` likewise uncataloged — but that topic is the already-documented orphan (H-22 / STR-05: deadman + ACK-SLA watchdog alerts with **no consumer**), so cataloging it is secondary to wiring its consumer | Audit sweep: `audit-events partitions=4 cleanup=BROKER_DEFAULT`, `lifecycle-alerts partitions=4 cleanup=BROKER_DEFAULT`; reset script topic tables (`scripts/kafka-reset-lab-topics.ps1:12-64`) contain neither. Orphan status: `docs/architecture-review/PHASE0-INVENTORY.md:79` | Low | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | Both topics added to the reset script's **ensure-tier** (create-if-missing, config-align, never delete — audit/alert data survives resets) with 7 d retention; live configs aligned via `kafka-configs --alter` | **Yes** — `--describe` shows `retention.ms=604800000, cleanup.policy=delete` as dynamic config on both | No |

Additional rows from Stages 3–4:

| ID | Pipeline/Stage | Symptom | Evidence | Severity | Category | Fix Applied | Fix Description | Re-verified | Approval Needed |
|----|----------------|---------|----------|----------|----------|--------------|------------------|-------------|------------------|
| PIPE-009 | Stage 3 / ACK lifecycle | The API-side `ACK_REQUESTED` lifecycle event is emitted with **scrambled fields**: `lifecycleState` and `eventType` carry the partition key (`<serverId>\|<sourceName>`), the state string `ACK_REQUESTED` lands in `detail`, and the human detail lands in `previousState`. Any consumer filtering `lifecycle-events` for `lifecycleState=ACK_REQUESTED` will never match — the requested-state is invisible on the wire | Live capture (topic `lifecycle-events`, run-tag vack5): `{"eventType":"f0af9a6d-…\|AckSim.vack5.Unit1.Tag_000", …, "lifecycleState":"f0af9a6d-…\|AckSim.vack5.Unit1.Tag_000","previousState":"Operator admin initiated ACK","detail":"ACK_REQUESTED"}`. Cause: `OperatorActionPublisher.cs:58-61` passes `(correlation, alarmId, partitionKey, state, detail)` positionally, but C# overload resolution binds the 5-arg call to the `EmitAsync` overload **without** `partitionKey` (`LifecycleEventPublisher.cs:57-64` binds all 5 exactly, beating the 7-param overload that needs a default) — every argument shifts one slot | Medium | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | `OperatorActionPublisher.cs:58` now uses named args (`lifecycleState:`, `detail:`) so overload resolution can only bind the partitionKey overload; the other call sites (line 94, AckSlaWatchdog) were verified to bind correctly already. ams-api image rebuilt + redeployed | **Yes** — live trace of one rack2 command through `lifecycle-events`: `ACK_REQUESTED` (detail="Operator admin initiated ACK") → `ACK_QUEUED` (prev=ACK_REQUESTED) → `ACK_CONFIRMED`, every field in its right slot. (Fixed events no longer leak sourceName into lifecycleState — greps must key on commandId now.) `ACK_PENDING_DCS`-never-emitted doc drift remains open (Low) | No |
| PIPE-013 | Fix round / topic catalog | `live.alarm.metrics` (LiveStateJob's STR-12 alarm-metrics output) was **never in the topic catalog** — it only ever existed via broker auto-creation. The moment auto-create went off (PIPE-005 fix), the sink's producer blocked on missing metadata at checkpoint flush and the **Live State RBE job entered a checkpoint-expiry restart loop** (failed=30, restored=15; re-emitting its backlog on every restore) | Job exceptions: `Exceeded checkpoint tolerable failure threshold … Checkpoint expired before completing`; `kafka-topics --list \| grep live.alarm.metrics` = absent; sink target `LiveStateJob.java:45` (`ALARM_METRICS_TOPIC`), topic in no catalog | Medium (surfaced by the PIPE-005 fix — previously silent) | auto-fix | **Yes** | Topic created per live.* conventions (4 partitions, 7 d retention, delete, lz4) + added to `kafka-reset-lab-topics.ps1` with an explanatory comment | **Yes** — job resumed completing checkpoints and drained its backlog after topic creation (see Stage log fix-round entry) | No |
| PIPE-014 | Fix round / IoTDB historian | **IoTDB server memory exhaustion → silent alarm-historian data loss.** From ~23:08 local every `insertRecords` failed with `301 … not enough memory to execute current fragment instance (free 24419B, needed 131072B)` — for ~40 min the alarm historian (Flink `IoTDBSink`), `RawLoopIotDbConsumer`, and cplm-api KPI writes all failed. Worse: `IoTDBSink` **logs the error and drops the batch** while the Flink job keeps completing checkpoints and committing source offsets — the at-least-once claim (`IoTDBPersistenceJob.java:27`) is upheld only by the sink's **in-memory** batch list: after the IoTDB restart the retained batches re-flushed and every stalled-window alarm landed (verified: rfeed1/rack2/rlive2 all present post-recovery) — but a TaskManager restart during such an outage WOULD lose them permanently, because the source offsets are already committed | TaskManager log: `ERROR org.apache.iotdb.flink.IoTDBSink - flush error` + `StatementExecutionException: 301 … not enough memory…` repeating every 30 s flush; consumer group `flink-ams-iotdb-persistence` LAG 0 (offsets committed while writes failed); `count timeseries root.ams.site1.alarms.*rfeed1*` = 0. IoTDB has no explicit heap sizing in compose (`docker-compose.yml:74-95`, image defaults) | **High** (silent data loss in a historian) | mixed → **CLOSED 2026-08-17 (hardening round)** | **Yes** | (a) IoTDB heap sized explicitly: `infra/docker/iotdb/datanode-env.sh` mounted over the stock script (`ON_HEAP_MEMORY=3G` / `OFF_HEAP_MEMORY=1G` — env vars can't override; the script recalculates unconditionally). (b) New `FailLoudIoTDBSink` replaces the upstream connector sink in `IoTDBPersistenceJob`: batches flush inside `snapshotState()` and THROW after 3 retries — a failed write now fails the checkpoint, so source offsets never advance past unpersisted records | **Yes** — heap: `-Xmx3G` live. No-loss gold test: IoTDB **stopped**, 5 alarms injected, **TaskManager restarted mid-outage** (the previously-fatal move — buffer destroyed), IoTDB restarted → **5/5 alarms present in IoTDB** (replayed from Kafka); the outage was loudly visible as `FailLoudIoTDBSink.snapshotState` checkpoint failures in the TM log | No |
| PIPE-010 | Stage 6 / live path | `live.alarms` (and by construction all `KafkaSinks.valueOnly` outputs) carry **null keys** — records for one alarm round-robin across the topic's 4 partitions, so the edge node has no per-alarm ordering guarantee (an UPSERT and a CLEAR for the same alarm can be consumed out of order under load) | Observed live: 8/8 `vlive1` records on `live.alarms` with null key (`kafka-console-consumer print.key`). Sink: `LiveStateJob` via `KafkaSinks.valueOnly` (working-tree `KafkaSinks.java`) — the 2026-08-13 sink fix keyed only the compacted-topic sinks | Low | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | `LiveStateJob` sinks now `KafkaSinks.keyedByJsonField(…, "alarmId")` (local unkeyed helper deleted); `LoopLiveRbeJob` via new `CplmKafkaSink.attachKeyed(…, "loopId")` | **Yes** — live tail of `live.alarms` shows every record keyed (`SIM-rlive2-006\|{…}`, flood replays `FLD-…\|{…}`); end-to-end run rlive2: 8 keyed RBE records + contract-tier Redis snapshots | No |
| PIPE-011 | Stage 8 / binding resolution | Binding descriptors still return **pre-lockdown internal endpoints** a browser can never reach: `history.trendEndpoint/rawEndpoint = http://historian-bff:8090/…`, `alarm.signalRHub = http://ams-api:8000/hubs/alarms`, `alarm.alarmApiEndpoint = http://ams-api:8000/…`, `live.mqtt.host = emqx:8083` — all compose-network addresses, unpublished since the Plan 04 gateway lockdown. Harmless today only because the frontend ignores those fields (it consumes `ioTDbPath`/sparkplug device+metric/`redisSnapshotKey` and builds `/api/hist/*`, `/hubs/*`, `/mqtt-ws` itself — `TrendCore.tsx`, `useBindingResolver.ts`), but the descriptor contract documented in `04-data-flows.md` §3 ("MQTT WS + …", "/api/hist/* URLs") is not what the service returns, and any NEW consumer trusting the descriptor gets dead URLs | Live resolve output run-tag vbind6 (see `pipeline-reports/sim_binding_resolution.json`); resolver source `src/services/binding-resolver/Services/PathResolver.cs` | Low | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | `PathResolver.cs` now emits gateway-relative URLs from `Public:*` config (defaults `/api/hist/*`, `/hubs/alarms`, `/api/v1/alarms/...`); `MqttConnectionInfo` gains `wsPath` (default `/mqtt-ws` — the browser transport; host/port stay for in-cluster consumers). binding-resolver image rebuilt + redeployed | **Yes** — live resolve returns `trend=/api/hist/trend?series=…`, `hub=/hubs/alarms`, `api=/api/v1/alarms/active?…`, `snap=/api/hist/snapshot?assets=…`, `wsPath=/mqtt-ws`; `sim_binding_resolution` run-tag `rbind1` 7/7 | No |
| PIPE-012 | Stage 11 / flood detection | **`FloodDetectFilter` is unreachable dead code for http-feed alarms** — the documented behavior "drop severity ≥ 950" (`05-flink-jobs.md`) can never fire on the primary feed path: `ValidationMap` first replaces the event's severity with the priority band floor (`priorityToSeverity`, max = CRITICAL → **900**, `PipelineOperators.java:63,473-482`), and only then does `FloodDetectFilter` test `severity < 950` (`PipelineOperators.java:368`). A 500-alarm burst sent with severity 960 / priority CRITICAL sailed through: all 500 reached `current-alarm-state` at severity 900 | Live flood run (run-tag vfld1): 2000 distinct alarms incl. 500 at severity 960 → CAS grew by exactly 2000; severity histogram of the run's CAS records = `1500×700, 500×900`, zero ≥950. Code path: `PipelineOperators.java:62-69` (clamp) precedes `:364-369` (filter) | Medium | flag → **approved & fixed 2026-08-13 (fix round, option a)** | **Yes** | `RawOpcAlarmEvent` gains `rawSeverity` (wire value captured in `ValidationMap` before the priority-floor clamp); `FloodDetectFilter` now tests `max(severity, rawSeverity) >= 950`. Restores the documented contract with minimal change. The deeper design question (severity-drop vs **rate-based** flood handling per EEMUA-191 — the current filter still silently discards the highest-severity alarms by design) remains open for a product decision | **Yes** — `sim_stress_flood --expect-flood-drop` run rfld2 7/7: 100 severity-960 records dropped, `current-alarm-state` grew by exactly 300 (= distinct − flood band), zero 960s on the topic, RBE + EMQX bounded, no checkpoint failures | Rate-based redesign only |
| PIPE-007 | Stage 4 / alarm historian | The two Flink consumers of `raw-alarms` normalize CRITICAL to **different severities**: the state machine writes 900 to Postgres/console while the IoTDB persistence job writes **950** to the historian — the same alarm reads severity 900 in the Alarm Console and 950 in the Trend/history tree. 950 is also exactly the FloodDetectFilter drop threshold (`severity >= 950` dropped, `PipelineOperators.java:368`), so the historian records CRITICAL alarms at a value the state machine treats as flood-band | `src/flink/.../PipelineOperators.java:476` (`case "CRITICAL": return 900`) vs `src/flink/.../IoTDBPersistenceJob.java:200` (`case "CRITICAL": return 950`). Observed live: alarm `SIM-vfeed3-004` (sent priority CRITICAL) = severity **900** in `alarms.alarm_current` + `/api/v1/alarms/active`, severity **950** at `root.ams.site1.alarms.SIM_vfeed3_004.severity` | Medium | flag → **approved & fixed 2026-08-13 (fix round)** | **Yes** | `IoTDBPersistenceJob.priorityToSeverity` CRITICAL now returns **900** (with a PIPE-007 comment), matching `PipelineOperators`; jar rebuilt, job resubmitted | **Yes** — live: `root.ams.site1.alarms.SIM_rfeed1_004` = `severity=900, priority=CRITICAL` (was 950 for the same input pre-fix); Postgres shows 900 for the same alarm — historian and projection now agree | No |

## Pre-flight checklist result (§3)

| Check | Result |
|---|---|
| 1. Stack up | ✅ 34 containers, all core services healthy (`docker ps` 2026-08-13). Stack was already running (not restarted for this run) |
| 2. Flink 7 standing jobs RUNNING | ✅ All 7 supervisor jobs RUNNING. Total overview: 10 RUNNING (7 standing + `AMS - Analysis Execution Engine` + `AMS Alarm State Export Engine` + `AMS - Alarm KPI Engine`) + 2 CANCELED remnants (`AMS - Alarm KPI Engine` 730c748…, `AMS - Alarm State Machine` a85a6d0…) from the 2026-08-13 sink-fix redeploy — cosmetic, expected. **NB:** the running Alarm State Machine / Alarm KPI Engine come from the *working-tree* jar (KafkaSinks keying fix, `docs/alarm-history-flink-sink-stuck.md`) hot-copied into the containers; images not yet rebuilt |
| 3. Kafka topics per catalog | ✅ All catalog topics exist (see Stage 1 audit below for config-level results). One non-catalog topic found: `lifecycle-alerts` (investigated in Stage 1) |
| 4. `iotdb-init` completed | ✅ `ams-iotdb-init: Exited (0) 4 hours ago` |
| 5. Postgres schemas | ✅ All 8 databases present (`ams`, `traverse_{assets,auth,displays,templates,analysis,audit,cplm}`); `traverse_cplm` has `cpm.*` + `analytics.*` schemas. Volume was NOT fresh for this run (stack pre-existing) — baseline data present: `alarms.alarm_current`=82, `alarms.alarm_history`=86 rows (includes 2026-08-13 sink-fix session data). Noted, not assumed clean |
| 6. CPLM consumer groups singleton | ⚠️ `ams-api-cplm-results`: exactly 1 member ✓. `ams-api-cplm-frames`: group does not exist → PIPE-003 |

Additional pre-flight facts recorded for later stages:
- Registered CPM loops (`traverse_cplm.cpm.loop_registry`): `G13_LOOP_A`, `G13_LOOP_B` (TIC, site1/unit1), `B2_027PIC` (PIC, site1/B2). Sims target these.
- ACK contract: Flink accepts Pascal- or camelCase on `operator-actions` (`OpcEventStreamJob.java:199-228`), emits `ackState=ACK_DISPATCHED` to `ack-writeback`; `ack-results` `ResultState=ACK_CONFIRMED` → `ACK_STATE_UPDATE` upsert keyed by `alarmId`.
- EMQX: host 1883, edge creds `ams_edge`/`changeme_edge` (lab `.env`); Sparkplug group/edge `ams_site1`/`ams_edge1`.
- Auth: `POST :8081/api/auth/login` (`admin`/`ChangeMe123!`) → `token`; service key `traverse-internal-dev-key` accepted by binding-resolver/historian-bff/analysis (not display-service).

## Production hardening round (2026-08-17 — user-approved: "no data loss in prod / what breaks from restarting")

Five items, implemented in the requested order, each verified live:

| # | Item | What changed | Verification |
|---|---|---|---|
| 1 | **Flink JM HA** | ZooKeeper-backed HA on JM+TM (`high-availability.type: zookeeper`, root `/flink-ams`, cluster `/ams-lab`, storageDir `s3://ams-flink/ha/` on MinIO — the STR-02 groundwork made this the promised "config change") | `docker restart ams-flink-jobmanager` → REST back in 18 s, **all 10 jobs recovered with IDENTICAL job ids** (registry survived), SM `restored=1` (state from checkpoint), **0 supervisor submissions post-restart**, CAS offsets unchanged (7250→7250). Before HA the same restart wiped the registry and cost all keyed state |
| 2 | **IoTDB durability (PIPE-014 closure)** | Explicit heap via mounted `datanode-env.sh` (`-Xmx3G` verified live); new `FailLoudIoTDBSink` (flush inside `snapshotState()`, throw after 3 retries → checkpoint fails → Kafka replays) | **Gold test passed**: IoTDB stopped → 5 alarms injected → **TM restarted mid-outage** → IoTDB restarted → 5/5 alarms in the historian. That exact sequence was permanent silent loss before |
| 3 | **Recovery-state-aware submitters** | `flink-job-supervisor.sh` + `flink-submit-cplm.sh` + `flink-submit-raw-alarms.sh` now count `CREATED/INITIALIZING/RUNNING/RESTARTING/RECONCILING` as present (RUNNING-only checks double-submitted restart-looping/recovering jobs — observed 2026-08-13). Correction: the supervisor already owned KPI/Export/Analysis (STR-07/08) — `05-flink-jobs.md` was stale (said 7 standing; it's 10) and is fixed; `ensure_flink_jobs.py` CORE_JOBS brought to parity | Fleet at 10 RUNNING with **zero duplicates** through a JM restart + a TM restart + an SM cancel/resubmit in one session |
| 4 | **Committed-offsets start** | `--raw-alarms.starting-offsets committed` (committed-with-earliest-fallback) in `OpcEventStreamJob` (+ its operator-actions/ack-results helper sources) and `IoTDBPersistenceJob`; compose supervisor env + `ensure_flink_jobs.py` updated | Cancel SM → supervisor fresh-submits → **CAS grew by 0** (an earliest replay would have been ~+6,000). ACK/operator-action history no longer re-dispatches on fresh submits either |
| 5 | **RF≥3 (prep — physically impossible on 1 broker)** | `kafka-reset-lab-topics.ps1` replication now env-parametrized: `KAFKA_TOPIC_RF` / `KAFKA_TOPIC_MIN_ISR` (lab default 1/1; prod runs 3/2 per `docs/ha-production-guide.md`). Lab stays single-broker by design | Script change verified by inspection; actual RF≥3 requires the prod 3-broker topology |

Post-hardening fleet: 10/10 RUNNING, no duplicates; residual `failed` checkpoint counts are the deliberate test perturbations (the IoTDB job's failures ARE the fail-loud sink refusing checkpoints during the induced outage). Remaining known-open (unchanged): rate-based flood-handling design decision (PIPE-012 note), `ACK_PENDING_DCS` doc drift (PIPE-009 note), prod-topology items (3-broker Kafka, ZK quorum, Postgres/IoTDB replication & backups).

## Fix round (2026-08-13, same day — user-approved "solve bugs and test again")

All flagged rows above were approved, fixed, redeployed, and re-verified (per-row evidence updated in place). Deployment: new Flink jar (`ams-flink:1.0-SNAPSHOT` image rebuilt → JM/TM/supervisor recreated), ams-api + binding-resolver images rebuilt + recreated, Kafka recreated with the new listener/auto-create env, new `mock-dcs` service. Re-run results:

| Re-verification | Result |
|---|---|
| `sim_alarm_feed` rfeed1 (12) + rlive2 (8) + rhist1 (4) | ✅ all checks, all runs |
| `sim_ack_lifecycle` rack2 | ✅ 10/10 — **real** `ACK_REQUESTED → ACK_QUEUED → ACK_DISPATCHED → ACK_CONFIRMED` via mock-dcs, correct field placement, Postgres acked via round-trip in <2 s |
| `sim_live_mqtt_direct` rmqt1 | ✅ 7/7 |
| `sim_loop_samples` rloop2 | ✅ 11/11 (first attempt rloop1 failed only its 4 IoTDB legs → PIPE-014 memory exhaustion, re-ran clean after recovery) |
| `sim_binding_resolution` rbind1 | ✅ 7/7 with gateway-relative descriptors |
| `sim_stress_flood --expect-flood-drop` rfld2 | ✅ 7/7 — flood band actually drops now |
| Host Kafka client (PIPE-001) | ✅ list + produce via `127.0.0.1:9093` |
| Auto-create off (PIPE-005) | ✅ produce to nonexistent topic undelivered, nothing created |

Incidents during the fix round (both logged as rows): PIPE-013 (`live.alarm.metrics` uncataloged → checkpoint storm once auto-create went off; topic created + cataloged) and PIPE-014 (IoTDB memory exhaustion → historian writes failing silently; recovered by restart, durable sizing + fail-loud sink flagged). Operational notes: the supervisor double-submitted `Live State RBE` while it was restart-looping (`flink-job-supervisor.sh` counts only RUNNING) — duplicate cancelled; same race duplicated the manually-submitted KPI/Export jobs earlier — duplicates cancelled; **the state machine replays `raw-alarms` from earliest on a fresh submit** (supervisor arg), so `current-alarm-state` grew by ~6.4k replayed upserts after the JobManager registry wipe — idempotent in Postgres, no duplicate rows. Doc-staleness: `04-data-flows.md` §7 says AnalysisExecutionJob is "not in compose supervisor" — it is now (`flink-job-supervisor.sh:118`).

Still open after the fix round: PIPE-014's durable items (IoTDB memory sizing, fail-loud IoTDB sink), the rate-based flood-handling design question (PIPE-012 note), and the `ACK_PENDING_DCS`-never-emitted doc drift (PIPE-009 note).

## Stage log

| Stage | Status | Notes |
|---|---|---|
| 1. Kafka topic audit | ✅ done | All 34 catalog topics exist; partitions match the reset script everywhere; `cleanup.policy` correct on every declared topic (`compact` on `current-alarm-state`, `kpi-standing-snapshots`, `alarm.state.active`, `ams.metadata.updates`, `context.parameter-set.v1`; `delete` elsewhere). Drift found + fixed: PIPE-004 (3 topics on broker defaults → aligned). Flagged: PIPE-005 (auto-create on), PIPE-006 (`audit-events`/`lifecycle-alerts` uncataloged) |
| 2. Flink job health | ✅ done | All 7 standing jobs RUNNING with checkpoint mode+interval exactly per `05-flink-jobs.md`: SM exactly_once/30s, IoTDB at_least_once/60s, LiveState at_least_once/30s, CPLM short/fusion exactly_once/180s, long exactly_once/300s, LoopRBE exactly_once/60s. 0 failed checkpoints on every job (SM 114/114, IoTDB 293/293, LiveState 586/586). Extras healthy: Analysis at_least_once/60s, Alarm KPI exactly_once/60s, Alarm State Export no-CP (documented). Known accepted gap: Kafka sinks deliver at-least-once even under EXACTLY_ONCE checkpointing (documented in 05, not a bug) |
| 3. Pipeline A (alarm feed) | ✅ pass (`sim_alarm_feed` run-tag `vfeed3`) | 12/12 alarms: keyed `ALARM_STATE_UPSERT` on `current-alarm-state` (all keys = alarmId — sink-fix confirmed working), 12 Postgres `alarm_current` rows with correct identity columns, severity→priority mapping correct in `/api/v1/alarms/active` (12/12), SignalR OnNewAlarm/OnAlarmUpdated fired (12 events, via direct-WS `skip_negotiation`), `lifecycle-events` grew. E2E latency ≈ 30–50 s (bounded by 30 s checkpoint flush). Note: for http-feed events Flink **replaces** numeric severity with the priority band floor (`PipelineOperators.java:63`) — design behavior, sim expectation adjusted. SignalR negotiate-then-connect through the gateway 404s ("No Connection with that ID") — see PIPE-008 in Stage 10 notes (direct WS works; the frontend uses WS transport) |
| 4. Historian path (alarms → IoTDB) | ✅ pass (with PIPE-007) | All 12 alarms at `root.ams.site1.alarms.SIM_vfeed3_*` (12/12 `severity` series), keyed by **sanitized Kafka alarmId** (`SIM-vfeed3-000` → `SIM_vfeed3_000`), NOT the sourceName — mapping confirmed as documented. Measurements severity/state/ack_status/condition_active/priority/source_name/condition_name present with correct values. Defect: CRITICAL severity written as 950 here vs 900 in Postgres → PIPE-007 (flagged) |
| 3b. ACK lifecycle (`sim_ack_lifecycle` run-tag `vack5`) | ✅ pass (10/10 checks; PIPE-002/PIPE-009 noted) | Real path: `POST /acknowledge/batch` (3 GUIDs) → **`ack_status` NOT set directly by the API** ✓ → `operator-actions` → Flink → `ack-writeback` ×3 `ackState=ACK_DISPATCHED` ✓ → `HttpAckWritebackService` POST → `Connection refused (192.168.1.51:8010)` → `ack-results` `ACK_FAILED` ✓ → `lifecycle-events` `ACK_QUEUED`,`ACK_FAILED` ✓. Confirm branch (synthetic DCS reply into `ack-results`): `ACK_CONFIRMED` → `current-alarm-state` `ACK_STATE_UPDATE` ✓ → Postgres `ack_status=t` 3/3 ✓. Note: batch endpoint takes row **GUIDs** (`items[].id`), not string alarmIds. Defect: ACK_REQUESTED event field-scramble → PIPE-009 |
| 5. Pipeline B (live, isolated) | ✅ pass (`sim_live_mqtt_direct` run-tag `vmqt3`; 7/7 checks) | 6 records into `live.alarms` (Flink bypassed) → sparkplug-edge-node → EMQX: DDATA for 6/6 devices in <10 s; **DBIRTH before first DDATA** per device ✓; alias map declared in DBIRTH (aliases 0–8: alarmId, severity, state, acknowledged, conditionActive, priority, sourceName, conditionName, message) and all DDATA aliases resolvable ✓; QoS 0 ✓; retain flag never set ✓. Redis snapshots on the **contract tier** (`ams-redis-contract`, edge `REDIS_HOST=redis-contract`) with TTL ✓ — first sim attempt scanned `ams-redis` and found nothing: worth knowing for ops tooling. No IoTDB write for these alarms (`IOTDB_PERSIST` unset) ✓ |
| 6. Full live path via Pipeline A | ✅ pass (`sim_alarm_feed` run-tag `vlive1`) | 8 raw alarms → state machine → `LiveStateJob` RBE → `live.alarms` grew 249→257 (**exactly 8**) → edge node → EMQX + Redis: 48 snapshot keys (8 devices × 6 metrics), TTL ≈ 3500 s. Full-loop closure of the seam Stage 5 bypassed. Observation → PIPE-010: `live.alarms` records from `LiveStateJob` are **null-keyed** (fine for delete-policy topic, but no per-alarm partition ordering across its 4 partitions) |
| 7. CPLM / loop path | ✅ pass (`sim_loop_samples` run-tag `vloop1`; 11/11 checks) | 2×1200 samples (G13_LOOP_A/B, event time compressed over 45 m so the 15-min event-time timers + fusion fire in-run). `clpm.feature.short.v1` 0→597, `clpm.feature.long.v1` 0→18, `clpm.gate.results.v1` 0→12; `traverse_cplm` `analytics.cplm_short_feature_results` and `analytics.cplm_gate_results` grew; IoTDB raw `root.site1.cpm.<loop>.{pv,sp,op,vp,mode}` — **exact-count** increases (+1200 per loop, so no duplicate `(series,ts)` points = idempotency ✓); cplm-api KPI dual-write produced the full `kpi.{gate_24h,gate_12h,short_1m…60m,long_4h/12h/24h}.*` tree; `live.loop.metrics` 0→7046 (per-metric RBE); forbidden `CplmGateStreamJob` not running ✓. Latency: short features within ~5 s of publish; gate results within ~10 s |
| 8. Binding resolution | ✅ pass (`sim_binding_resolution` run-tag `vbind6`; 7/7 checks) | role=live → MQTT/Sparkplug topic+device+metric+`redisSnapshotKey` ✓; role=history → `ioTDbPath = root.<path with /→.>` ✓ (the field the frontend actually consumes, `TrendCore.tsx:128-160`); role=alarm → SignalR hub + alarms API + `live.alarms` ✓; `POST /resolve/batch` (`{"bindings":[…]}` contract, `BindingRequest.cs:22-24`) resolves 3/3 ✓; **edge-only auth verified**: bare `X-Service-Key` and anonymous both 401 at the gateway ✓. Observation → PIPE-011: descriptor still returns pre-lockdown internal URLs |
| 9. API surface smoke | ✅ pass | Against synthetic data, via gateway w/ JWT: `/api/v1/alarms/active` (SimFeed rows present), `/api/v1/alarms/active/statistics` (141 active incl. run data), `/api/v1/cpm/loops` (registry), `/api/v1/cpm/loops/G13_LOOP_A/gates` (**24h windows from the vloop1 run**), `/api/v1/cpm/loops/G13_LOOP_A/kpis` (99 samples), `/api/hist/trend` + `/api/hist/raw` (`series=root.site1.cpm.G13_LOOP_A&measurements=pv&start&end` — points returned), `/api/hist/snapshot?assets=SimFeed_vlive1_Unit2_Tag_007` (Redis metric map incl. severity/state) — all 200, all reflecting injected data, not stale state. Note: pipeline health lives at `/api/v1/health/pipeline` (200, kafka/flink healthy); bare `/health/pipeline` is 404 (the prompt's shorthand) |
| 10. Frontend smoke | ✅ pass (transport-level) | SPA shell + hashed bundles serve on `:3000` for all routes (`/dashboard`, `/alarms`, `/live-events`, `/trend`, `/edge`, `/system` → 200). Data planes each page consumes verified live: SignalR `/hubs/alarms` via gateway (Stage 3, 12 events), **`/mqtt-ws` MQTT-over-WebSocket via BOTH nginx `:3000` and gateway `:8081`** — 401 without JWT (edge auth enforced on WS ✓), connects with `?access_token=` (the mechanism `mqttStore.ts:279-286` uses) and delivered 193 Sparkplug messages in 6 s; historian (`/api/hist/*`) + `/api/v1/health/pipeline` (the `/system` page's source) verified in Stage 9. Caveat: no browser DOM render was driven (no Playwright in the lab) — visual confirmation not covered, transport + data planes fully covered |
| 11. Flood/load | ✅ infra pass; 1 product defect (PIPE-012) | Burst vfld1: **8000 records in 3.8 s (2114 msg/s)**, drained through the state machine in ~48 s. DedupFilter collapsed 5× identical repeats (8000 in → 2000 CAS upserts, exact); RBE emitted exactly 1 `live.alarms` record per distinct alarm (2000); EMQX fan-out bounded (2000 DDATA observed, no unbounded growth); **all 7 standing jobs RUNNING throughout, 0 new checkpoint failures**. Defect: the 500 severity-960 records were NOT flood-dropped → PIPE-012. Re-run vfld2 (corrected expectations): 6/6 checks pass |
| Cross-cutting checks | ✅ done | **CQRS**: no JDBC/Postgres sink anywhere in `src/flink` (grep: only a dead `DB_URL` default in `PipelineConfig.java:82`, consumed by no sink; `OpcEventStreamJob.java:22` comment holds). **CPLM singleton**: `ams-api-cplm-results` and `ams-api-cplm-results-frames` each exactly 1 member (static `-sole-` instance ids) before, during, and after all tests; frames lag 0 after consuming the run's gate results. **Idempotency**: byte-identical replay of the 12 vfeed3 `raw-alarms` records (captured from the topic incl. keys, re-produced verbatim) → `alarm_current` **12 → 12** and `alarm_history` **12 → 12** (Flink DedupFilter dropped all 12 replays on same-ts state; nothing reached projection); IoTDB loop writes exact-count (Stage 7). **Auth boundary**: in-network probes from `ams-sim` → `ams-api` 401 (no auth AND bogus JWT — own JWKS enforced), `display-service` 401, `asset-model` 401 without key AND 401 with the *dev-default* key (lab runs a 48-char non-default `Auth__ServiceKey`; binding-resolver attaches it outbound, `Program.cs:27-29`) — internal hop authenticated, dev default rejected. Gateway edge: anonymous + bare service key + tokenless WS all 401 (Stages 8, 10) |
